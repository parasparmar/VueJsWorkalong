
@{
    ViewData["Title"] = "NEO v1";
}

<h1>NEO - Near Earth Objects</h1>


<table class="table table-bordered table-striped">
    <thead class="table-light">
        <tr>
            <th>#</th>
            <th>Date</th>
            <th>NEO Name</th>
            <th>Close Approach Date</th>
            <th>Miss Distance</th>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody v-cloak>
        <tr v-for="(a,name,index) in asteroids">
            <td>{{index + 1}}</td>
            <td>{{a.name}}</td>
            <td>{{name}}</td>
            <td>{{a}}</td>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
            <td></td>
        </tr>
    </tbody>
</table>

@section Scripts{
    <script type="text/javascript">
        const apiKey = "MKTeRhimZ3uiDIQWZF6824vWYhzwk2gva3lrqLIb";
        var url = "";

        var vm = new Vue({
            el: "#app",
            data: {
                asteroids: [],
                dates: []
            },

            created: function () {
                // Run this function as soon as the page is created.
                this.fetchAsteroids();
            },

            methods: {
                // Uses NASA's Astronomy API to fetch Near Earth objects like asteroids and Comets.
                fetchAsteroids: function () {
                    url = "https://api.nasa.gov/neo/rest/v1/feed?start_date=START_DATE&end_date=END_DATE&api_key=" + apiKey;
                    url = url.replace("START_DATE", "2020-08-31");
                    url = url.replace("END_DATE", "2020-08-31");

                    axios.get(url).then(function (res) {
                        debugger;
                        vm.dates = Object.keys(res.data.near_earth_objects);
                        vm.dates.forEach(a => vm.asteroids.push(vm.dates[a].near_earth_objects));
                        //vm.asteroids = res.data.near_earth_objects;
                    });
                }

                //Get Close Approach date
                // getCloseApproachDate(a)

            }
        });
    </script>
}